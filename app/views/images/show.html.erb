<p id="notice"><%= notice %></p>
 <% if(@image.flayer.present?)%>
        <%=image_tag(@image.flayer,:size => "400x400") %>
    <% else %>
    <%end%>
<%= link_to 'Edit', edit_image_path(@image) %> |
<%= link_to 'Back', images_path %>
